# 前端需求文档

## 1. 项目背景

当前项目是一套面向英语文本语料的研究型批处理工具包，核心能力是按 `source/<类别>/` 批量读取 `.txt` 文本，调用 `run_metrics.py` 及 `metric_modules/` 中的各计算模块，输出多维句法复杂度指标 CSV。现有使用方式以命令行为主，对不熟悉命令行、虚拟环境、配置文件和依赖状态的研究用户不够友好。

本前端的目标是为现有批处理流程提供一个轻量、直观、可跨平台运行的桌面窗口，使用户能够通过图形界面完成资源安装、数据选择、计算方法配置、任务运行、进度查看和结果打开，而不需要直接编辑命令或手动调用脚本。

## 2. 产品目标

- 提供独立桌面窗口，不依附浏览器标签页或命令行终端。
- 支持 Windows 10（1809 及以上）、Windows 11、macOS、Linux。
- 面向非工程背景研究用户，降低配置、运行和查看结果的操作门槛。
- 保留现有命令行管线的可复现性：前端只负责配置、调度和展示，不改写核心指标算法。
- **安装包保持轻量**：默认安装包只包含前端壳、运行时引导器（`uv`）、后端脚本与资源清单，不内置 Python 运行时、Python 依赖、Stanza/UDPipe 模型、Java 与 Stanford 解析器。
- **资源按需安装**：Python 运行时、Python 依赖、模型与外部工具均由页面内“资源管理器”下载、校验、安装到应用数据目录；未就绪的计算方法不可运行。
- 界面轻量、响应快、动画克制但有质感，适合研究工具而非营销页面。
- 界面支持中英双语切换：多语言机制预留扩展能力，成品只包含中文与英文两种语言包。

## 3. 用户与典型场景

### 3.1 目标用户

- 语言学、二语习得、心理健康文本分析等方向的研究人员。
- 需要批量处理英文 `.txt` 语料并导出 CSV 指标的学生或助研。
- 希望复用当前工具包但不希望频繁接触命令行和配置文件的用户。
- 网络不稳定或离线环境下的研究用户。

### 3.2 典型流程

1. 用户打开桌面应用。
2. 首次启动进入资源管理器，查看各计算方法所需资源与下载量。
3. 选择“一键安装全部”或按需安装资源；等待下载、校验与安装完成。
4. 选择项目目录或语料目录。
5. 查看应用自动识别到的类别文件夹和文本数量。
6. 选择计算方法：`custom`、`leo`、`quansyn`、`neosca`。
7. 选择输出目录、是否断点续传、是否强制重算。
8. 点击开始运行。
9. 在界面中查看进度、日志、当前文件和耗时。
10. 运行结束后直接打开输出 CSV 所在目录，或在前端中预览结果表格。
11. 离线/弱网用户可从离线安装包导入资源，不访问网络即可完成安装。

## 4. 功能需求

### 4.1 项目与路径管理

- 支持选择工作区目录，默认识别当前项目结构。
- 支持选择输入目录，默认使用 `source/`，应用内可直接导入包含特定文件结构的压缩包处理。
- 支持选择输出目录，默认使用 `result/`。
- 支持直接从系统的文件管理器访问应用的输入输出目录。
- 自动扫描输入目录下的类别文件夹，并显示每类 `.txt` 文件数量。
- 对缺失目录、空目录、无 `.txt` 文件等情况给出明确提示。
- 支持将合法 `.zip` 直接导入到正式输入目录，导入前应校验结构并提示是否覆盖或合并。

### 4.2 方法配置

- 提供计算方法开关：
  - 自定义指标：`custom`
  - LeoDD Python 复刻：`leo`
  - QuanSyn：`quansyn`
  - NeoSCA：`neosca`
- 提供预设按钮：
  - `other`：启用 `custom + leo + quansyn`
  - `all`：启用全部方法
  - `neosca`：仅启用 NeoSCA
- 支持断点续传开关，对应当前 `resume` 行为。
- 支持强制重算开关，对应 `--no-resume`。
- 支持显示并保存当前配置到 `metrics_config.json`，保存前必须提示用户。
- 当关闭某个方法时，应提示其独有输出列可能不会生成。
- 提供输出列高级选择器，允许用户按启用方法细化最终 CSV 输出列。
- **方法就绪门控**：每个方法显示所需资源状态；任一必需资源未就绪时，方法不可启用，并给出原因与跳转资源管理器的入口。

### 4.3 运行控制

- 提供开始、取消/停止按钮。
- 运行中禁用可能破坏当前任务的配置项。
- 支持实时显示：
  - 当前类别
  - 当前文件
  - 已完成数量 / 总数量
  - 当前阶段，例如 Stanza、LeoDD、QuanSyn、NeoSCA、写入 CSV
  - 运行日志
- 支持任务失败时保留日志，并提示失败原因和可能的处理方式。
- 支持应用内控制台（只读）。
- 取消任务时强制终止后端进程树（Windows 上包括 Java 子进程），不等待优雅退出；已完成输出依赖现有断点续传机制处理。
- 点击按钮或切换关键状态后，如果 1 秒内没有完成反馈，应显示转圈、骨架屏或其他等待提示。

### 4.4 资源管理器

资源管理器是本前端第一版的核心页面，负责所有运行时与外部资源的下载、校验、安装、修复和卸载。

#### 4.4.1 资源分组

- Python 运行时与依赖（`custom`/`quansyn` 需要）：受管 CPython 3.11、CPU 版 torch、stanza、quansyn、pandas、ufal.udpipe、conllu 等，按锁文件安装。
- 模型（按方法区分）：
  - UDPipe 英文模型 `english-ewt-ud-2.4-190531.udpipe`：`leo` 需要。
  - Stanza 英文模型：`custom`/`quansyn` 需要。
- NeoSCA 工具链（`neosca` 需要）：Temurin JRE、Stanford Parser 4.2.0、Stanford Tregex 4.2.0。

每项展示：名称、用途、对应方法、下载大小、磁盘占用、当前状态、版本。

#### 4.4.2 状态

- 每项状态：未安装、下载中、已暂停、校验中、解压中、安装中、就绪、错误、版本不一致。
- 状态变化通过 JSONL 事件驱动 UI，前端不轮询文件系统。

#### 4.4.3 下载与安装

- 支持“一键安装全部”与单项安装。
- 下载要求：
  - 全部使用 HTTPS。
  - 支持断点续传（HTTP Range；服务端不支持时自动重新下载）。
  - 显示字节级进度；进度刷新节流，避免界面卡顿。
  - 支持暂停、继续、取消；取消后保留 `.part` 文件以便续传。
  - 失败自动重试（指数退避），并支持切换到备用镜像。
- 校验：下载完成后按资源清单校验 SHA-256，不通过则删除文件并重新下载。
- 解压：安全解压到应用数据目录，拒绝路径穿越、绝对路径、符号链接/硬链接；完成后原子写入正式路径。
- 安装前检查磁盘剩余空间，不足时提示所需空间。
- 所有资源安装到应用数据目录，不写入用户语料目录；不依赖系统已安装的 Python 或 Java。

#### 4.4.4 NeoSCA 专用下载器

- 不使用 `neosca` 包内置的依赖安装器，原因：
  - 其 `--yes` 模式仍会通过 `input()` 交互询问镜像选择，子进程环境会卡死。
  - 默认安装目录为 `USER_SOFTWARE_DIR`，在非 ASCII Windows 用户名下会落到盘符根目录，位置不可控。
  - Java 下载地址指向已停止维护的 `api.adoptopenjdk.net`。
  - 其 `extractall` 解压不防路径穿越。
- 由本项目自研下载器：按资源清单下载 Temurin JRE、Stanford Parser 4.2.0、Stanford Tregex 4.2.0，校验哈希后安全解压到应用数据目录，并设置 `JAVA_HOME`、`STANFORD_PARSER_HOME`、`STANFORD_TREGEX_HOME`、`PATH`。
- 安装后运行最小烟测：对一行英文样本执行 `python -m neosca`，检查退出码与输出文件；失败时提供日志与重装入口。

#### 4.4.5 校验、修复与卸载

- 提供“校验/修复”：检查文件存在、大小与哈希；损坏项重新下载。
- 提供“卸载/清理”：删除对应文件并释放磁盘；卸载前必须确认。
- 展示各资源磁盘占用与总占用。

#### 4.4.6 离线安装包

- 支持导入离线安装包（zip，内含资源归档与清单子集），校验哈希后离线安装，不访问网络。
- 离线包按平台与应用版本组织，版本不匹配时给出明确提示，避免混用。

#### 4.4.7 镜像与网络

- 每个可下载资源支持主地址与备用镜像，界面可切换。
- 网络错误给出可定位的错误信息、重试建议与镜像切换入口。

#### 4.4.8 环境清单

- 安装完成后生成 `environment.json`，记录应用版本、Python 版本、各包版本、模型版本、JRE 版本、安装时间与路径。
- 在“环境”页展示，供研究复现与问题排查。

### 4.5 日志与事件（JSONL）

- 后端任务与资源安装统一支持 `--log-format jsonl`，逐行输出 JSON 事件。
- 事件类型至少覆盖：任务开始/结束、进度（类别/文件/阶段/计数）、阶段切换、日志行、错误、资源状态（下载/校验/解压/安装）、环境自检。
- 默认人类可读输出保持不变；前端始终使用 JSONL。
- 日志节流与保存：应用日志保存到应用数据目录，限制单文件大小并滚动。

### 4.6 结果查看

- 运行完成后显示输出文件列表。
- 支持打开输出目录。
- 支持在前端中预览 CSV 的前若干行。
- 支持显示字段列名和样本数量。
- 支持复制输出路径。
- 初版不要求实现复杂统计图表，但应保留后续扩展空间。

### 4.7 多语言（i18n）

- 前端所有用户可见文本（页面标题、选项卡、按钮、提示、状态标签、错误摘要、资源名称等）支持中文与英文切换。
- 默认语言跟随操作系统语言（中文环境显示中文，其他环境显示英文），用户可在顶部栏手动切换，选择结果持久化，下次启动生效。
- 语言切换即时生效，无需重启应用。
- 语言包按 `zh-CN` 与 `en` 组织，结构允许未来增加其他语言，但成品只包含这两种。
- 后端原始日志与 JSONL 事件保持英文或结构化字段，不做强制翻译；前端负责将错误标题与建议显示为当前语言。
- 数字、百分比、日期与文件大小等格式化按当前语言环境展示，CSV 输出内容不受语言切换影响。

## 5. 非功能需求

### 5.1 跨平台与系统兼容

- Windows：
  - 最低支持 Windows 10 1809（含 LTSC 2019），兼容 Windows 11；x64 为首要目标，ARM64 后续评估。
  - WebView2 运行时：Windows 11 通常已内置；Windows 10 由安装器检测，缺失时静默安装内置的 Evergreen Bootstrapper，失败则给出下载地址。
  - 支持中文、空格、括号路径与非 ASCII 用户名；启用长路径支持（`longPathAware`）。
- macOS：
  - 支持 Apple Silicon 与 Intel（arm64/x64 分开发行）。
  - 应用需要签名与公证；运行时下载的组件需通过哈希与代码签名校验，并处理 Gatekeeper 隔离属性。
- Linux：
  - x64；依赖系统 WebKitGTK（Tauri 2 要求），该依赖无法在应用内安装，需在安装说明中明确。
  - 提供 `.deb` 与 AppImage；AppImage 文档中列出所需系统库。
- 所有平台：路径分隔、进程树终止、环境变量注入行为按平台实现并纳入测试。

### 5.2 安装包体积与首次安装

- 默认安装包目标：不含 Python 运行时、模型与 Java，体积控制在 50MB 以内（含 Tauri 壳、`uv`、Windows WebView2 Bootstrapper 与资源清单；最终以打包实测为准）。
- 首次安装下载量按所选方法显示预估，例如 `custom`/`quansyn` 依赖约数百 MB（以锁文件实测为准），`neosca` 另需 JRE 与 Stanford 工具。
- 首次安装不是强制的全量安装：用户可只安装所需方法的最小集合；安装完成前对应方法置灰。

### 5.3 轻量与响应速度

- 应避免使用过重的桌面壳层，除非打包和依赖集成收益明显。
- 长任务与资源安装均在后台进程或工作线程中执行，前端主线程保持响应。
- 前端界面渲染与文件扫描、依赖检查、压缩包导入、任务运行、下载进度刷新不得在同一线程上执行。
- UI 操作如切换页面、勾选方法、打开配置面板应接近即时响应。
- 日志与下载进度刷新应节流，避免大批量输出导致界面卡顿。

### 5.4 视觉与交互

- 视觉风格应简洁、清晰、研究工具导向。
- 使用多选项卡页面组织流程，不把所有操作挤在同一个界面。
- 使用明确的图标、分组、进度条、状态标签和轻量动画。
- 建立统一控件体系，例如通知、报错、确认对话框、按钮组、状态标签、进度条和表格。
- 动画应服务于状态变化，不应影响性能。
- 应有明暗主题或至少提供舒适的低对比疲劳配色。
- 界面文本支持中英双语切换，切换即时生效并记忆用户选择。

### 5.5 稳定性与可恢复性

- 下载中断可续传；校验失败可重试；安装失败不留下“半可用”状态（原子替换 + 安装状态文件）。
- 前端崩溃不应破坏已下载的 `.part` 文件与已完成的 CSV 输出。
- 任务日志应可保存，便于排查失败原因。
- 对路径中包含空格、中文或特殊字符的情况应进行测试。
- 对大型语料应保持可取消、可恢复、可查看进度。

### 5.6 安全

- 全部下载使用 HTTPS；所有资源固定 URL + SHA-256，安装前强制校验。
- 未通过校验的文件不得执行或解压。
- 解压必须防路径穿越（拒绝 `../`、绝对路径、符号链接/硬链接）。
- 优先使用官方签名二进制（`uv`、Temurin JRE）；Windows SmartScreen 与 macOS Gatekeeper 作为发布风险处理并在文档中说明。

### 5.7 可复现性

- Python 依赖使用锁文件（`requirements.lock` / `uv.lock`）固定精确版本与哈希；torch 固定 CPU 版并指定官方 CPU 索引。
- 模型、JRE、Stanford 工具固定版本并记录在资源清单与 `environment.json`。
- 资源页提供“环境自检”：对 `source/text` 使用 `other` 预设输出到临时目录，与 `result/text.csv` 对比满足 `mismatches []`；开发与发布验收时必须通过。

## 6. 技术方向

### 6.1 最终技术方向（方案 A：轻量安装包 + 页面内按需安装）

- Tauri 2 提供独立窗口、文件选择、进程管理与打包能力。
- 前端使用 Svelte 5 + TypeScript + Vite 构建轻量界面；自建设计令牌与统一控件库，不引入重型组件框架。
- 前端内置轻量 i18n：语言包按 `zh-CN`、`en` 组织，默认跟随系统语言，可手动切换并持久化。
- 安装包内置：Tauri 壳、`uv`（按平台固定版本与哈希）、后端脚本（纯标准库）、Windows WebView2 Bootstrapper、资源清单。
- 首次引导流程：Tauri 调用 `uv` 下载受管 CPython 3.11 → 在应用数据目录创建 venv → 按锁文件安装依赖（CPU 版 torch）。
- Python 后端两类入口：
  - 任务入口：`run_metrics.py --log-format jsonl`，复用现有管线，不改写算法。
  - 资源入口：`scripts/resource_manager.py`（纯标准库），负责模型、JRE、Stanford 工具安装，输出 JSONL。
- NeoSCA 使用自研下载器，不使用 `neosca` 内置安装器。

### 6.2 自动打包脚本

应提供统一打包脚本 `scripts/package_app.py`，由用户选择目标平台后自动执行对应构建流程：

- 构建前端资源。
- 复制 `uv`、后端脚本与资源清单。
- Windows 附加 WebView2 Bootstrapper 与长路径配置。
- 生成并校验平台资源清单。
- 调用 Tauri 打包命令（Windows NSIS、macOS dmg、Linux deb/AppImage）。
- 输出安装包位置与体积，写入构建日志。

## 7. 验收标准

- 在至少 Windows 10 1809/22H2 与 Windows 11 干净环境生成可运行独立窗口应用；安装包不含 Python 运行时、模型与 Java，体积达标（以 5.2 为准）。
- 首次启动资源管理器可下载安装全部资源，进度、暂停/继续、校验失败重试可用。
- 资源就绪前对应方法不可启用；资源就绪后 `other` 预设生成的 `text.csv` 与现有 `result/text.csv` 一致（`mismatches []`）。
- 自研 NeoSCA 下载器在干净环境可完成 JRE + Parser + Tregex 安装并跑通烟测；安装目录位于应用数据目录，不写入盘符根目录或 `AppData\Roaming` 根目录。
- JSONL 事件驱动前端进度、日志与资源状态；人类可读输出不受影响。
- 取消任务会终止包括 Java 在内的进程树；下载取消后断点续传可用。
- 离线安装包可在无网络环境完成安装。
- 中文路径、空格路径、非 ASCII 用户名下任务可运行。
- macOS（arm64/x64）与 Linux 冒烟通过，或安装说明明确给出系统依赖。
- 界面支持中英双语切换：默认跟随系统语言，手动切换即时生效且重启后保持；主要页面与提示文案均无遗漏的硬编码中文。

## 8. 风险与待确认事项

- `uv`、Python、torch CPU wheel 在各平台的可用性与下载体积（以锁文件实测为准）。
- JDK 版本选择：默认 Temurin 17；若 NeoSCA/Stanford Parser 4.2.0 在 17 下异常，回退 Temurin 8（实施时烟测确认）。
- 资源清单中的真实 URL 与 SHA-256 由开发期打包脚本生成并维护；上游地址（Stanford、LINDAT、Adoptium）变更会导致安装失败，需要镜像兜底。
- macOS 公证/隔离与 Windows SmartScreen 对运行时下载组件的影响。
- Linux WebKitGTK 属于系统依赖，无法在应用内安装。
- 首次安装强依赖网络；弱网用户依赖离线包与断点续传。
- Windows/Linux ARM64 支持优先级暂缓。
